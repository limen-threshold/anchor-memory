chromadb>=0.4.0
sentence-transformers>=2.2.0

# Chinese / Japanese word segmentation for the keyword lane.
# v1.14: required, not optional. Without it CJK text has no word boundaries,
# the whole phrase becomes one LIKE %...% token, and keyword search on CJK
# memories almost never matches. Harmless for pure-English data.
jieba>=0.42
